// Insert models once created below
const { Pet, User } = require('../models');

const resolvers = {
  // GET CALLS
  Query: {
    // insert queries
  },
  // POST CALLS
  Mutation: {
    // insert mutations
  },
};

module.exports = resolvers;
